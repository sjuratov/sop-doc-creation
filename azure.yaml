# Application name
name: sop-doc-creation
infra:
  provider: bicep
services:
    app:
        project: src/frontend
        host: containerapp
        language: python
        docker:
            path: Dockerfile
            remoteBuild: true